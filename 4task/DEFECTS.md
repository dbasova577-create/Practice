# Найденные дефекты

| ID | Дефект | Риск | Исправление и защита от регрессии | Статус |
|---|---|---|---|---|
| BUG-001 | Отрицательное число взрослых принималось как валидное | Некорректная или отрицательная стоимость | Валидация `adults >= 1`; параметризованный unit-тест | Исправлен |
| BUG-002 | Отрицательное число детей принималось как валидное | Занижение стоимости | Валидация `children >= 0`; unit-тест | Исправлен |
| BUG-003 | Отрицательная цена за ночь принималась как валидная | Отрицательная итоговая стоимость | Запрет отрицательной цены; unit-тест | Исправлен |
| BUG-004 | Сообщение об ошибке содержало опечатку `Ощибка` | Некачественный диагностический контракт | Единые проверяемые сообщения валидации | Исправлен |
| BUG-005 | Python `round()` использует округление к чётному: `round(0.5) == 0` | Ошибка на денежных значениях ровно в половину рубля | `Decimal` + `ROUND_HALF_UP`; тест результата `0.5 -> 1` | Исправлен |
| BUG-006 | Путь к JSON зависел от текущей рабочей директории | Тесты падали при запуске не из корня репозитория | Путь вычисляется относительно `case_loader.py`; интеграционный тест меняет CWD | Исправлен |
| BUG-007 | CI показывал покрытие, но не контролировал минимум 80% | Pipeline оставался зелёным при деградации покрытия | `--cov-fail-under=80` и `fail_under = 80` | Исправлен |
| BUG-008 | Coverage report не сохранялся как результат pipeline | Нельзя скачать и проверить детальный отчёт | XML + HTML отчёты загружаются как GitHub Actions artifact | Исправлен |

